render.yamlservices:
  - type: web
    name: nursing-system-web
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: EDC_IP
        value: "你的EDC實體IP"
      - key: DATABASE_URL
        fromDatabase:
          name: nursing-db
          property: connectionString

databases:
  - name: nursing-db
    plan: free
