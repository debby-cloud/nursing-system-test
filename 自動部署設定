render.yamlservices:
  - type: web
    name: nursing-system-web
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: EDC_IP
        value: "http://60.251.229.32"
      - key: DATABASE_URL
        fromDatabase:
          name: nursing-db
          property: connectionString

databases:
  - name: nursing-db
    plan: free
